@model Categorie

<div class="col-md-4">
    <div class="thumbnail">
        <img src="@Model.Icon" alt="poza pentru magazin haine" />

        <div class="card-body">
            @*<h3 class="pull-right">@Model.Pret.ToString("c")</h3>*@
            <h4><a asp-controller="Produs" asp-action="ListaBarbati" asp-route-id="@Model.CategorieId">@Model.NumeCategorie</a></h4>
            <p>@Model.DescriereCategorie</p>
        </div>
        @*<div class="addToCart">
            <p class="button">
                <a class="btn btn-primary"
                   asp-controller="ShoppingCart"
                   asp-action="AdaugaInShoppingCart"
                   asp-route-produsId="@Model.ProdusId">Adauga in Cos</a>
            </p>
        </div>*@
    </div>
</div>